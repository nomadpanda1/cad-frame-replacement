# 案例七：多图框逐框替换（程序化生成可控样本）

| 子样本 | 说明 | 结论 |
|--------|------|------|
| 07a 平铺多框（含整图纸框） | A1 整图含 3 个子图框；整图纸框 (0,0)-(841,594) 识别为**纸边**、非替换目标 | 每子框独立插入 `HH_FRAME_A1`，纸边保留；删旧框线+旧标题栏(各 12 实体)，无重叠 ✅ |
| 07b 并排多框（无整图纸框） | 4 个并排 A3 图框，无单一整图纸框（CNG 真实场景简化版） | 4 框均为替换目标，逐框抽取+回填全部成功 ✅ —— 验证 `detect_frames_hierarchical` 的「无整图纸框」分支 |

## 输出
`outputs/`：07a / 07b 的 `before` / `template` / `HH` PNG + `results.json` + `index.html`。
